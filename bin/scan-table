#!/usr/bin/env python

import boto3
import os
import sys

# So we can import from parent directory
sys.path.insert(1, os.path.join(sys.path[0], '..'))
from backend import dynamodb

table_name, region, dynamodb_url = dynamodb._get_config()
dynamodb = boto3.client(
    'dynamodb', region_name=region, endpoint_url=dynamodb_url)

print dynamodb.scan(TableName=table_name)
