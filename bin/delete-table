#!/usr/bin/env python

import boto3

dynamodb = boto3.client(
    'dynamodb', region_name='us-west-2', endpoint_url="http://localhost:8000")

table = dynamodb.delete_table(TableName='FleetStatus')

# print("Table status:", table['TableDescription']['TableStatus'])
print("Table deleted.")
